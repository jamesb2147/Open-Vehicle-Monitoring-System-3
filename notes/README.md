# Working notes — not for upstream

This branch (`notes/fiat500e-working-notes`) exists only to keep working
documents in version control. It is **deliberately kept off the PR branches**
so that `fiat500e/stability-and-climate-separation` and
`boltev/fix-polling-list-delete` stay clean for upstream review.

Do not merge this branch upstream.

| File | What it is |
|---|---|
| `fiat500e-battery-temp-issue.md` | Draft GitHub issue for the one decode defect that could not be fixed without a vehicle capture |
| `esp-idf-build-setup.md` | How to rebuild the ESP-IDF toolchain for this project, including the non-obvious parts |

## Related branches

```
boltev/fix-polling-list-delete              1 commit
fiat500e/stability-and-climate-separation   6 commits
```

Both are pushed to `jamesb2147/Open-Vehicle-Monitoring-System-3`.
